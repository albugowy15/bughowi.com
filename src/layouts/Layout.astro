---
import "@/styles/global.css";
import Footer from "@/layouts/Footer.astro";
import NavbarMenu from "./NavbarMenu.astro";
import { Font } from "astro:assets";
import profile_pic from "@/assets/profile.png";
import { Image } from "astro:assets";

interface Props {
  title: string;
  description: string;
  url?: string;
  type?: "website" | "article";
  img?: string;
}

const links = [
  { title: "Blog", path: "/blog/page/1" },
  { title: "Projects", path: "/projects/page/1" },
  { title: "Snippets", path: "/snippets/page/1" },
  { title: "About", path: "/about" },
];

const {
  title,
  description,
  url = "https://bughowi.com",
  type = "website",
  img = "https://res.cloudinary.com/bughowi-assets/image/upload/c_thumb,w_200,g_face/v1692602156/home-thumbnail.png",
} = Astro.props;
---

<html lang="en">
  <head>
    <Font cssVariable="--font-roboto" preload />
    <title>{title} - Mohamad Kholid Bughowi</title>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Mohamad Kholid Bughowi's Personal Blog"
      href={new URL("rss.xml", Astro.site)}
    />
    <meta name="msapplication-TileColor" content="#da532c" />
    <meta name="theme-color" content="#090909" />

    <meta property="og:url" content={url} />
    <meta property="og:type" content={type} />
    <meta property="og:title" content={`${title} - Mohamad Kholid Bughowi`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={img} />

    <!-- X Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="twitter:domain" content="bughowi.com" />
    <meta property="twitter:url" content={url} />
    <meta name="twitter:title" content={`${title} - Mohamad Kholid Bughowi`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={img} />

    <script
      defer
      is:inline
      src="https://umami.yawilab.com/script.js"
      data-website-id="d29eddf8-7b6d-455a-96d3-29d42c0cd906"></script>
    <script
      defer
      is:inline
      src="https://umami.yawilab.com/recorder.js"
      data-website-id="d29eddf8-7b6d-455a-96d3-29d42c0cd906"></script>
  </head>
  <body>
    <div class="flex min-h-screen w-full flex-col">
      <a
        href="#main-content"
        class="sr-only focus-visible:not-sr-only focus-visible:fixed focus-visible:top-3 focus-visible:left-3 focus-visible:z-50 focus-visible:rounded-md focus-visible:bg-background focus-visible:px-4 focus-visible:py-2 focus-visible:text-foreground focus-visible:ring-2 focus-visible:ring-ring"
      >
        Skip to main content
      </a>
      <header class="sticky top-0 z-30 hidden w-full py-3 md:block lg:py-6">
        <div class="container mx-auto px-3 xl:px-14 2xl:max-w-7xl">
          <nav
            class="flex justify-between items-center px-4 py-3 lg:px-5 lg:py-4 rounded-full backdrop-blur-md text-sm sm:text-base bg-background/60 border"
          >
            <a href="/" title="Homepage" aria-label="Homepage">
              <Image
                src={profile_pic}
                width={30}
                height={30}
                loading="eager"
                priority
                class="aspect-square"
                alt="Mohamad Kholid Bughowi"
              />
            </a>
            <div class="hidden md:flex items-center gap-2">
              {
                links.map((link) => (
                  <a
                    href={link.path}
                    class="cursor-pointer text-foreground/70 px-1 py-0.5 font-bold transition-colors duration-300 hover:text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background"
                  >
                    {link.title}
                  </a>
                ))
              }
            </div>
          </nav>
        </div>
      </header>
      <NavbarMenu links={links} />
      <main
        id="main-content"
        class="container mx-auto flex-1 px-3 pt-8 pb-28 sm:pt-10 md:pb-10 lg:py-12 xl:px-14 2xl:max-w-7xl"
      >
        <slot />
      </main>
      <Footer />
    </div>
  </body>
</html>
